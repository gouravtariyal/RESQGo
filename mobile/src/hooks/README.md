# hooks

Shared React hooks used across multiple features (e.g. permissions, app state, debouncing).

Feature-specific hooks belong under `features/<feature>/hooks`. Prefer thin hooks that compose `services/` and `store/`.
