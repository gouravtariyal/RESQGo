# api

Top-level HTTP/API contracts and client configuration for RESQGo.

Keep transport concerns here (request helpers, endpoint maps, response transformers). Feature modules consume this layer; do not put UI or screen logic in this folder.
